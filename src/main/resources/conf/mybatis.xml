<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE configuration
        PUBLIC "-//mybatis.org//DTD Config 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-config.dtd">
<configuration>

    <!--设置别名-->
    <typeAliases>
        <!--实体类所在的包名-->
        <package name="com.zyc.entity"/>
    </typeAliases>

    <!--sql映射文件所在的位置-->
    <mappers>
        <!--name是包名，包中的所有mapper.xml文件都能一次加载-->
        <package name="com.zyc.dao"/>
    </mappers>
</configuration>